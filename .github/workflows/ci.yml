name: new_release
on:
  push:
    branches:
      - master
    tags:
      - v*

jobs:

  setup:
    steps:
      - uses: actions/checkout@v2

      - uses: s-weigand/setup-conda@v1
      - run: conda --version
      - run: which python


  build:
    steps:
      - name: First step
        run: echo Yo


  testing:
    steps:
      - name: First step
        run: echo Yo

  deploy:
    steps:
      - name: First step
        run: echo Yo
